interface:
  display_name: "Release Rommel en doe wat"
  short_description: "Build, copy, and commit the Windows installer"
  default_prompt: "Use $release-rommel-en-doe-wat to build the Windows installer, copy it to the marketing download path, and commit that installer update."
